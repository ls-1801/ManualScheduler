import { JobStateFilterPipe } from './job-state-filter.pipe';

describe('JobStateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobStateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
